ALARM_SOUND_PATH=/sounds/bell.mp3   # mount your file to this path in the container
AUDIO_DEVICE=                  # Linux only, e.g. plughw:CARD=Device,DEV=0
PORT=3000

# MCP server
MCP_TRANSPORT=stdio   # stdio (local) or httpStream (networked, e.g. Raspberry Pi)
MCP_PORT=3001
DINGDING_API_URL=http://localhost:3000
INSTANCE_NAME=Kitchen
